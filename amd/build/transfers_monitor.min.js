define("repository_largefile/transfers_monitor",["exports"],(function(_exports){Object.defineProperty(_exports,"__esModule",{value:!0}),_exports.init=void 0;
/**
   * Live-refresh the Transfers page's "uploads in progress" region.
   *
   * Polls a read-only, admin-gated endpoint (transfers.php?ajax=uploads) every few
   * seconds and swaps just that region's markup in place, so a chunked upload —
   * including a Background Fetch upload that continues after its owner closed their
   * tab — shows its progress climbing without the admin reloading the whole page
   * (which would also disturb the "queue a new transfer" form). Polling pauses while
   * the tab is hidden, and a transient fetch failure simply waits for the next tick.
   *
   * @module     repository_largefile/transfers_monitor
   * @copyright  2026 Vernon Spain
   * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
   */
let timer=null,generation=0;const schedule=(url,region,interval,mygen)=>{timer=window.setTimeout((async()=>{"visible"===document.visibilityState&&await(async(url,region)=>{try{const response=await fetch(url,{credentials:"same-origin"});if(!response.ok||response.redirected)return;let data;try{data=await response.json()}catch(e){return}if(!data||"string"!=typeof data.html)return;const target=document.getElementById(region);target&&(target.innerHTML=data.html)}catch(e){}})(url,region),mygen===generation&&schedule(url,region,interval,mygen)}),interval)};_exports.init=config=>{const url=config.url,region=config.region,interval=config.interval||5e3;null!==timer&&window.clearTimeout(timer),generation++,schedule(url,region,interval,generation)}}));

//# sourceMappingURL=transfers_monitor.min.js.map